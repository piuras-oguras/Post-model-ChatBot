[project]
name = "post-model-release"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "llama-index-core>=0.12",
    "llama-index-llms-openai-like>=0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.8",
    "pytest>=8.3",
]
